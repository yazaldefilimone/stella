use super::Checker;
use crate::ast::ast;
// use crate::diagnostics::{Diagnostic, TypeError};
// use crate::types::Type;

impl Checker {
  pub fn check_declaration_statement(&mut self, local: &ast::DeclarationExpression) {
    // implement declaration statement checks
  }
}
